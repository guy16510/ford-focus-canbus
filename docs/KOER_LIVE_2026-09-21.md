# Live KOER transcript — 2026-09-21

Vehicle: 2013 Ford Focus gasoline, blade key

The evidence-backed Ford KOER request was accepted by the live PCM in extended session:

```text
10 01 -> 50 01 00 32 01 F4

preflight:
RPM      813 rpm
Speed    0 km/h
Coolant  59 C
Runtime  39 sec
Voltage  14.04 V

10 03 -> 50 03 00 32 01 F4
31 01 02 82 -> 71 01 02 82 22
```

`0x22` is Ford Type-2 RoutineInfo = active, confirming RID `0x0282` and the UDS request shape on this PCM.

While the routine remained active, repeated result requests returned:

```text
31 03 02 82 -> 7F 31 21
```

`0x21` here is the UDS negative-response code `busyRepeatRequest`; it is distinct from the Ford Type-2 RoutineInfo byte `0x21` that would mean an aborted routine in a positive `71 ...` response.

The old runner retried `0x21` up to five times every 0.75 seconds inside each poll cycle, generating unnecessary traffic. The corrected runner sends only one `31 03 02 82` result request per poll interval and treats `7F 31 21` as the already-accepted KOER routine still being busy.

Ford service material for KOER says the test is performed at normal operating temperature and can include driver-operated input checks such as brake-pedal application/release, steering input where applicable, and transmission-control input where equipped. The runner now displays those operator actions after `71 01 02 82 22` instead of silently polling while the driver waits.

Safety remains unchanged: no foil or ignition cycling until an explicit completed Type-2 result is decoded.
